# @offlid/adapter-claude-code

offlid agent adapter for the **Claude Code CLI**. Implements the `AgentAdapter`
interface from [`@offlid/adapter-sdk`](../../adapter-sdk): how to launch the
agent, where its session transcript lives, and how the session maps onto a
capture manifest.
