# @offlid/adapter-codex

offlid agent adapter for the **Codex CLI**. Implements the `AgentAdapter`
interface from [`@offlid/adapter-sdk`](../../adapter-sdk): how to launch Codex,
where its session transcript lives, and how the session maps onto a capture
manifest.
