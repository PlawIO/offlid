{
  "name": "offlid-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Open-core client for offlid: the always-on checkpoint daemon for coding agents.",
  "license": "Apache-2.0",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "turbo run build",
    "lint": "biome check .",
    "format": "biome format --write .",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "changeset": "changeset",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "@changesets/cli": "^2.27.0",
    "turbo": "^2.1.0",
    "typescript": "~5.6.0"
  }
}
